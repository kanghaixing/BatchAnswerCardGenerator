"""
答题卡批量生成配置文件

坐标说明：
- 本配置使用 PDF 常见的 pt 点坐标。
- x 从页面左侧开始计算。
- y 这里统一用“距离页面顶部”的坐标，便于对照预览图调整。
- A4 页面大约是 595 x 842 pt。

如果更换模板，通常只需要修改这里的坐标。
"""

CONFIG = {
    # Excel 列名候选。程序会自动在这些候选里找对应列。
    "excel_columns": {
        "name": ["姓名", "学生姓名", "考生姓名", "name"],
        "student_id": ["识别号", "考号", "准考证号", "学生编号", "id", "student_id"],
    },

    # 字体设置。STSong-Light 是 ReportLab 内置中文 CID 字体，跨平台相对稳定。
    "font_name": "STSong-Light",
    "font_size": 12,

    # 需要用白色覆盖掉的旧内容区域：左上 x、顶部 y、宽、高
    "white_masks": [
        # 原姓名/考号文字区域。不要盖住左右四个定位小黑块。
        {"x": 50.2, "y": 34.0, "w": 110.0, "h": 36.5},
        # 原二维码区域。
        {"x": 517.8, "y": 18.8, "w": 41.7, "h": 41.7},
    ],

    # 考试名称区域
    "exam_title": {
        "label": "考试名称：",
        "x": 50.0,
        "baseline_y": 22.0,
        "line_start_x": 103.0,
        "line_end_x": 165.0,
        "line_y": 24.0,
        "line_width": 0.9,
        # 如果运行时传入 --exam-name，会写在这个位置；默认不写，保留横线供手写。
        "value_x": 104.0,
        "value_baseline_y": 22.0,
    },

    # 学生姓名、识别号文字位置
    "student_text": {
        "name_x": 51.0,
        "name_baseline_y": 48.0,
        "id_x": 51.0,
        "id_baseline_y": 67.0,
        "name_prefix": "考生姓名：",
        "id_prefix": "识别号：",
    },

    # 二维码位置：内容为识别号
    "qr": {
        "x": 518.0,
        "y": 19.0,
        "size": 41.5,
        "border": 1,
    },

    # “考生姓名 / 识别号”区域附近四个视觉定位点
    # 原始点略小，为避免印刷或渲染看起来残缺，这里重绘为 6.8pt 实心方块。
    "locator_squares": {
        "size": 6.8,
        "items": [
            {"x": 45.3543, "y": 31.1811},
            {"x": 168.3780, "y": 31.1811},
            {"x": 45.3543, "y": 66.3307},
            {"x": 168.3780, "y": 66.3307},
        ],
        # 这些定位点原模板约 4.5354pt。这里保持中心点不变，放大重绘。
        "original_size": 4.5354,
    },
}
